# Frontend

Simple Vite + React + shadcn starter for the transcoding pipeline.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

## Notes

- The app is scaffolded with TypeScript, Tailwind CSS, and a shadcn-ready folder structure.
- Add more shadcn components with the CLI when you’re ready.

